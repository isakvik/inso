priority list for beta

known discrepancies
    slider mechanics leniency from lazer
    no hidden sliderticks
    we reuse reversearrows instead of spawning new ones
    circle and overlay blend individually
        this seems like an optimization on osu's side that creates a single texture from circle and overlays?

old features
    cursor ripple
    slider body color from combo color
    sliderball tint

skinning
    cursormiddle element + smooth interpolated trail for cursormiddle skins (osu.odin cursor_trail_draw)
    cursor expand on click, CursorExpand/CursorRotate/CursorCentre ini keys
    skin.ini AnimationFramerate (sliderball hardcoded to 1s loop; judgements/followpoints use own timing)
    remaining ini keys: SliderBallFlip, HitCircleOverlayAboveNumber, LayeredHitSounds
    score/combo number fonts (ScorePrefix, ComboPrefix, default-x/dot/percent glyphs) - scorescreen uses none of these yet
    beatmap skin images
    followpoint leadin animation (tween written but disabled, osu_hitobjects.odin followpoint_emit)

extra features
    rotate slider from head
    mine elements

scripting
    animated drawable - pass a spritesheet

TEST
    custom buffers
    limit testing
    
gameplay
    non-scalar playfield scale
    score system
    replays

lua
    particle system

game
    drawable inspector
    offset wizard

platform
    lua profiling (nested profiler block support)
    builtin resource pack
    file watch parity across platforms
        linux recursive watch support
        windows unicode filename conversion

YEAST mode
    screen capture must work
    parse and apply settings from osu install (specify with path?), to allow warmup

    thepoon request:
    open map across all clients with keyboard input (enter, anything that works with stable)
