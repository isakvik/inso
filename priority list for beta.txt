priority list for beta

-- POST MAPPER ALPHA RELEASE -- 

osu editor integration (custom map_reference)

scripting
    Animated_Drawable - pass a spritesheet
    multiple custom drawables per step

rendering
    postprocessing
    render to FBO
    lua frame object

TEST
    mouse buttons when keyboard held down and vice versa
    notosu custom buffers
    custom animations
    limit testing
    
rendering
    locking bugfix
    3d pipeline support
    renderer limit handling
    
gameplay
    non-scalar playfield scale
    notelock
    score system
    replays
    
    finish animations
        texture array handling is kinda half baked
    
    phase 2 visuals
        cursortrail (mcosu uses a custom shader here)
        followpoints
        note stacking
        object dimming
    
lua 
    REPL console
    particle system

game
    ui core (low priority)
        map select
        skin select
        offset change
        audio settings
        
    entity hierarchy inspector
    offset wizard

platform
    lua profiling (nested profiler block support)
    input thread
    builtin resource pack
    file watch parity across platforms
        linux recursive watch support
        windows unicode filename conversion

beta hardening
    replace user/data-path asserts with recoverable errors
        map parsing
        config io

YEAST mode
    screen capture must work
    parse and apply settings from osu install (specify with path?), to allow warmup
    open specific map with command
        click circle to start? network sync?
    
    local networking for fun and video games
