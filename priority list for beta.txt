priority list for beta

-- PRE ALPHA --

-- POST MAPPER ALPHA RELEASE -- 

scripting
    draft more api

game mechanics (not score, replays, etc)
    per-hitobject size and approach rate
        
    finish animations
        texture array handling is kinda half baked

rendering
    render to FBO
    lua frame object

fixes
    reconsider entity ring buffer persistence

TEST
    mouse buttons when keyboard held down and vice versa
    notosu custom buffers
    custom animations
    limit testing
    


rendering
    locking bugfix
    bindless textures fallback
    fix the damn release build stuff
    3d pipeline support
    
gameplay
    notelock
    score system
    replays
    
    phase 2 visuals
        cursortrail (mcosu uses a custom shader here)
        followpoints
        note stacking
        object dimming
    
lua 
    REPL console
    particle system

game
    ui core (low priority)
        map select
        skin select
        offset change
        audio settings
        
    entity hierarchy inspector
    offset wizard

platform
    lua profiling (nested profiler block support)
    input thread
    builtin resource pack

YEAST mode
    screen capture must work
    parse and apply settings from osu install (specify with path?), to allow warmup
    open specific map with command
        click circle to start? network sync?
    
    local networking for fun and video games
