priority list for beta

bugs
    slider tracking doesnt play sound if head is ignored
    refresh while external map is loaded crashes game

bugs low priority
    scrubbing doesnt regen timed gameplay graphics (edit mode...)

old features
    reverse arrow animation
    combobreak sound
    cursor ripple

extra features
    slider snaking out
    mine elements

scripting
    Animated_Drawable - pass a spritesheet

TEST
    notosu custom buffers
    limit testing
    
rendering
    locking bugfix
    3d pipeline support
    
gameplay
    non-scalar playfield scale
    score system
    replays
    
    phase 2 visuals
        cursortrail (mcosu uses a custom shader here)
        followpoints
        note stacking
        object dimming

lua 
    REPL console
    particle system

game
    ui core (low priority)
        map select
        skin select
        offset change
        audio settings
        
    entity hierarchy inspector
    offset wizard

platform
    lua profiling (nested profiler block support)
    input thread
    builtin resource pack
    file watch parity across platforms
        linux recursive watch support
        windows unicode filename conversion

beta hardening
    replace user/data-path asserts with recoverable errors
        map parsing
        config io

YEAST mode
    screen capture must work
    parse and apply settings from osu install (specify with path?), to allow warmup

    thepoon request:
    open map across all clients with keyboard input (enter, anything that works with stable)
